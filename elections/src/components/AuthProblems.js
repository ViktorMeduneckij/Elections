import React from "react";

const AuthProblems = () => (
  <div className="text-center">
    <h1 className="text-xl">Oops, authorization did not go through.</h1>
    <p>It looks like there was problem with Metamask. Refresh and try again?</p>
  </div>
);

export default AuthProblems;
